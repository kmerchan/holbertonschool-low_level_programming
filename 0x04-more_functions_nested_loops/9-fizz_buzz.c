#include <stdio.h>

/**
 * main - print numbers 1-100 where multiples of 3 and/or 5 print words
 *
 * Return: 0 if success
 */

int main(void)
{
	/* c = 65 is 'A' and c = 90 is 'Z' */
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
